# Demo-only deps for Streamlit Community Cloud (takes precedence over the
# root requirements.txt for this entrypoint). This is the subset of
# pyproject.toml's real dependencies the demo's code path actually needs
# (dense retrieval, Chroma, no reranker) — pinecone/flashrank/rank-bm25
# are intentionally omitted, same as the real CLI only imports them lazily.
streamlit>=1.40,<2
langchain-core>=1.6,<2
langchain-google-genai>=4.4,<5
langchain-chroma>=1.1,<2
chromadb>=1.5,<2
pdfplumber>=0.11,<0.12
pydantic>=2.11,<3
python-dotenv>=1.1,<2
pyyaml>=6,<7
